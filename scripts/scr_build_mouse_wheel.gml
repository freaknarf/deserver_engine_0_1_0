if !keyboard_check(vk_f4)
{
if mouse_wheel_up() ///
if k<3 k++
if mouse_wheel_down() //// tant que le tableau existe on fait varier quelle instance il crée avec "k"
if k>0 k--
}


if keyboard_check(vk_f4)
    {
    
        if mouse_wheel_up() ///
        if tile<2 tile++
        if mouse_wheel_down() //// tant que le tableau existe on fait varier quelle instance il crée avec "k"
        if tile>0 tile--
    
    }